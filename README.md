# AI-projects-

Troy Dixon's workspace: the **TD Movement** portfolio site plus recruiting and AI working files.

```
.
├── index.html                  # Portfolio site (GitHub Pages entry — keep at root)
├── assets/
│   ├── images/                 # Site images (hero: puzzle_head.png)
│   └── documents/              # Public downloads (resume)
├── recruiting/
│   ├── job-descriptions/       # Nike GPS JDs + decoded summaries
│   ├── trackers/               # Candidate tracker PDFs
│   └── candidate-packets/      # ATS packets — git-ignored (PII)
├── ai-projects/                # Agents, workflows, experiments (one folder each)
├── docs/
│   └── screenshots/            # Site screenshots, notes
├── .claude/settings.json       # Claude Code harness config
└── CLAUDE.md                   # Instructions for Claude Code
```

## Site

Open `index.html` in a browser. No build step. Assets are referenced by relative path.
