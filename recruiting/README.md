# Recruiting

Nike GPS stretch-assignment working files (Principal Architect, Lead Event Planning, Inventory / Supply Chain).

- `job-descriptions/` — raw JDs and `/job-decoder` output (must-haves, sourcing keywords, screen questions).
- `trackers/` — Master Tracker–format candidate tracker PDFs (`/recruiter-embedded-tracker`).
- `candidate-packets/` — ATS packets. Git-ignored: contains candidate PII.
